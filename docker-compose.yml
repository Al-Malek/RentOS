version: '3.8'

services:
  app:
    build:
      context: .
      target: runner
    image: rentos:prod
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped

  app-dev:
    profiles: ["dev"]
    build:
      context: .
      target: dev
    image: rentos:dev
    command: npm run dev -- -H 0.0.0.0
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      NEXT_TELEMETRY_DISABLED: "1"
      WATCHPACK_POLLING: "true"
    volumes:
      - .:/app
<<<<<<< HEAD
      - rentos_node_modules:/app/node_modules
      - rentos_next_cache:/app/.next

volumes:
  rentos_node_modules:
  rentos_next_cache:
=======
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
    stdin_open: true
    tty: true
    restart: unless-stopped 
>>>>>>> 0be1bb153886424102e79b6bf1b1e34d02f1af8f
